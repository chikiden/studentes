Makefile — скрипт автоматической компиляции.

make

# Запуск статической сборки
./app_static

# Запуск динамической сборки (rpath настроен внутри Makefile, так что подхватится сразу)
./app_shared

# Запуск версии с dlopen
./app_runtime
